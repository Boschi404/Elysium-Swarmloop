# pytest configuration
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
addopts = -q --strict-markers
asyncio_mode = auto
markers =
    docker: marks tests that validate docker-compose.yml
    env: marks tests that validate .env files
    cross: marks cross-reference tests
